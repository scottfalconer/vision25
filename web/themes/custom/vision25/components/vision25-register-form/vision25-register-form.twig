<section{{ attributes.addClass('safe-frame', 'vision25-page-section', 'vision25-page-section--tight-top') }}>
  <div class="section-container vision25-register">
    <div class="vision25-register__card glass-card-elevated" data-scroll-reveal>
      {{ form }}
      <p class="vision25-register__note">{{ note }}</p>
    </div>
  </div>
</section>
