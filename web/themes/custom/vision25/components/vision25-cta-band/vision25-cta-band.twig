<section{{ attributes.addClass('gradient-anchor', 'safe-frame', 'vision25-cta-band') }}>
  <div class="section-container" data-scroll-reveal>
    <h2 class="heading-section">{{ title }}</h2>
    <p class="body-large">{{ subtitle }}</p>
    <p><a class="button button--light" href="{{ cta_url }}">{{ cta_label }}</a></p>
  </div>
</section>
